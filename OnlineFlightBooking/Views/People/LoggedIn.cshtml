@{
    ViewBag.Title = "LoggedIn";
}

<fieldset>
    <legend>
        <text>
            @if (Session["Username"] != null)
            {
                <text>
                    welcome! @Session["Username"].ToString();
                </text>
            }
    </legend>
</fieldset>
